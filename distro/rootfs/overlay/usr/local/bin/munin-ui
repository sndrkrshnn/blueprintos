#!/usr/bin/env bash
set -euo pipefail

BIN="/opt/muninos/bin/munin-ui"
if [[ -x "$BIN" ]]; then
  exec "$BIN" --host 0.0.0.0 --port 8080 --ui-dir /opt/muninos/ui
fi

echo "[munin-ui] missing native binary: $BIN" >&2
exit 1
